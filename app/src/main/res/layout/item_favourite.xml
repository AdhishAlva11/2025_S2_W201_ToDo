<?xml version="1.0" encoding="utf-8"?>
<!-- CardView layout for displaying a favourite location item -->
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp"
    android:layout_marginHorizontal="4dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="5dp"
    app:cardBackgroundColor="@color/white"
    app:cardUseCompatPadding="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="14dp"
        android:background="@drawable/favourite_card_background">

        <!-- Favourite Title -->
        <TextView
            android:id="@+id/txtFavTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/fav_name_label"
            android:textColor="@color/black"
            android:textStyle="bold"
            android:textSize="17sp"
            android:layout_marginBottom="4dp" />

        <!-- Favourite Address -->
        <TextView
            android:id="@+id/txtFavAddress"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/fav_address_label"
            android:textColor="#555555"
            android:textSize="14sp"
            android:layout_marginBottom="2dp" />

        <!-- Open/Closed Status -->
        <TextView
            android:id="@+id/txtFavStatus"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/fav_status_label"
            android:textColor="#777777"
            android:textSize="14sp"
            android:layout_marginBottom="2dp" />

        <!-- Rating Display -->
        <TextView
            android:id="@+id/txtFavRating"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/fav_rating_label"
            android:textColor="#FFA000"
            android:textSize="14sp" />
    </LinearLayout>
</androidx.cardview.widget.CardView>
